# spec Pipeline

## spec pipeline

`spec pipeline`은 advancedVSeed DSL을 입력받아 spec을 출력합니다.

`spec`은 VChart 또는 VTable의 입력 데이터 구조로, 차트의 구성 항목을 설명하는 데 사용됩니다.


`spec` 자체는 직렬화할 수 없으므로 Node.js 환경에서 빌드할 수 없으며 브라우저 환경에서만 사용할 수 있습니다.