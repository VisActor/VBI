# spec Pipeline

## spec pipeline

`spec pipeline` receives an advancedVSeed DSL and outputs a spec

`spec` is the input data structure of VChart or VTable, used to Description the configuration items of the chart


`spec` itself cannot be serialized, so it cannot be built in a Node.js environment and can only be used in a browser environment.