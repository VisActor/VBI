// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
  posts Post[]
}

model Post {
  id        Int     @id @default(autoincrement())
  title     String
  content   String?
  published Boolean @default(false)
  author    User    @relation(fields: [authorId], references: [id])
  authorId  Int
}

model Report {
  id        String   @id @default(uuid())
  name      String?
  data      Bytes
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([updatedAt])

  // Archived data only; no runtime Prisma Client API.
  @@ignore
}

model Chart {
  id        String   @id @default(uuid())
  name      String?
  data      Bytes
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([updatedAt])
}

model Insight {
  id        String   @id @default(uuid())
  name      String?
  data      Bytes
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([updatedAt])
}

model ReportCollaborationUpdate {
  id        BigInt   @id @default(autoincrement())
  reportId  String
  data      Bytes
  merged    Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([reportId, id])
  @@index([reportId, merged])

  // Archived data only; no runtime Prisma Client API.
  @@ignore
}

model ChartCollaborationUpdate {
  id        BigInt   @id @default(autoincrement())
  chartId   String
  data      Bytes
  merged    Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([chartId, id])
  @@index([chartId, merged])
}

model InsightCollaborationUpdate {
  id        BigInt   @id @default(autoincrement())
  insightId String
  data      Bytes
  merged    Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([insightId, id])
  @@index([insightId, merged])
}
